# name: First workflow
# on:
#   push:
#     branches: 
#       - main

# jobs:
#   job1:
#     runs-on: ubuntu-latest
#     steps:
#       - name: Print Hello
#         run: echo "Hello from Job 1"
#   job2:
#     runs-on: ubuntu-latest
#     steps:
#       - name: Print Hello
#         run: echo "Hello from Job 4"


name: "Task : Security check and Environment verification"
on : [push]
jobs:
  security-check:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v2
      - name : Security Check
        run: | 
          if [ "${{secrets.PASSWORD}}" == "" ]; then
            echo "Critical : Password not set"
            exit 1

          else echo "Security Check Passed"
          fi
  version-check:
    runs-on: ubuntu-latest
    steps:
      - name: Terraform version check
        uses: hashicorp/setup-terraform@v3
        with:
          terraform_version: "1.7.0"
      - name: Check Terraform version
        run: terraform version
  summary-report:
    runs-on: ubuntu-latest
    needs: [security-check, version-check]
    steps:
      - name: Generate summary report
        run: |
          echo "Security Check Summary"
          echo "======================="
          echo "Password Check: ${{ steps.security-check.outputs.result }}"
          echo "Version Check: ${{ steps.version-check.outputs.result }}"
          echo "Workflow triggered by : ${{github.actor}}"
          echo "Repository : ${{github.repository}}"
